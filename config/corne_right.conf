# Right half — touchpad-specific Kconfig.
# CONFIG_I2C is inherited from shared corne.conf.

# Enable the stelmakhdigital TPS43 driver.
# AYM1607's CONFIG_INPUT_AZOTEQ_IQS5XX no longer applies — different driver,
# different Kconfig symbol.
CONFIG_INPUT_TPS43=y
