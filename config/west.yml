manifest:
  defaults:
    revision: v0.3
  remotes:
    - name: zmkfirmware
      url-base: https://github.com/zmkfirmware
    - name: stelmakhdigital
      url-base: https://github.com/stelmakhdigital
  projects:
    - name: zmk
      remote: zmkfirmware
      import: app/west.yml
    # TPS43-specific driver: has proper ZMK PM integration (suspend <1uA idle),
    # datasheet-correct reset timing (610ms ATI wait), retry on NACK outside
    # comm window. See TOUCHPAD_HANDOFF.md R1.
    - name: zmk_driver_azoteq
      remote: stelmakhdigital
      revision: master
  self:
    path: config
