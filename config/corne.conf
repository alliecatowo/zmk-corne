# USB output (required explicitly for split keyboards)
CONFIG_ZMK_USB=y

# Enable ZMK Studio (no locking so we can always connect)
CONFIG_ZMK_STUDIO=y
CONFIG_ZMK_STUDIO_LOCKING=n

# Pointing device support
CONFIG_ZMK_POINTING=y
CONFIG_ZMK_POINTING_SMOOTH_SCROLLING=y

# I2C for touchpad
CONFIG_I2C=y
CONFIG_I2C_LOG_LEVEL_DBG=y
CONFIG_INPUT_LOG_LEVEL_DBG=y

# NOTE: do NOT set CONFIG_LOG_MODE_IMMEDIATE=y alongside CONFIG_ZMK_USB_LOGGING.
# USB CDC doesn't enumerate for ~1s after boot; immediate-mode logs before that
# are dropped. Deferred mode (USB_LOGGING's default) buffers them and flushes
# once USB is live, so we actually see iqs5xx init messages.
CONFIG_LOG_BUFFER_SIZE=16384

# Increase BT transmit power for better split connectivity
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y

# --- Sleep / power / battery ---
# NOTE: CONFIG_ZMK_SLEEP=y gated on battery. When enabled, the keyboard enters
# system_off after 15min idle and wakes on keypress. Fine (and essential) on
# battery; disruptive on USB-only (brief reconnect delay every 15 min idle).
# Uncomment when a battery goes in.
# CONFIG_ZMK_SLEEP=y
# CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=900000

# Slow battery ADC poll from default 60s to 120s. Harmless on USB-only.
# (When batteries aren't physically installed, ADC reads return near-zero
# and ZMK just reports 0% — no crashes, no other side effects.)
CONFIG_ZMK_BATTERY_REPORT_INTERVAL=120
